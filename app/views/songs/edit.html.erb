<h2>Edit a song!</h2>

<%= render 'form' %>

	<!--<%= form_for(@song) do |f|%>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :artist %>
	<%= f.text_field :artist %>

	<%= f.submit %>
	<% end %>-->